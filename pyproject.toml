[project]
name = "monitorhub"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "django>=4.2,<5.0",
    "django-environ",
    "PyMySQL",
    "channels",
    "channels-redis",
    "redis",
    "daphne",
    "msal",
    "requests",
    "Pillow",
]
